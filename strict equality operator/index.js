// = assignment operator
// == comparison operator
// === strict equality operator

// Declare a variable and assign a string value to it
let x = "3.14";

// Use the strict equality operator (===) to compare x with the numeric value 3.14
if (x === 3.14) {
    console.log("That is pi");
} else {
    console.log("That is NOT pi");
}
